{% extends 'base.html.twig' %}

{% block body %}
    <div class="container up-main">
        <div class="row">
            <div class="col-md-12">
                <div class="row">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Typ</th>
                            <th>Edycja</th>
                            <th>Stwórz</th>
                        </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Podstrony</td>
                                <td><a class="btn btn-info" href="{{ path('unipro-admin-list', {'repository': 'PagesContent'}) }}">
                                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                    </a></td>
                                <td><a class="btn btn-success" href="{{ path('unipro-admin-new') }}">
                                        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                                    </a></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

